#include <list>

using namespace std;

class Solution {
public:
    int numSubarrayProductLessThanK(vector<int>& nums, int k) {
        int count = 0;
        list<int> li;
        for(int i=0; i<nums.size(); i++) {
                        
        }
    }
};