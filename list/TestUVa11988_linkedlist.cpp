#include <cstdlib>
#include <list>
#include <cstring>
#include <cstdio>
using namespace std;

const int maxn = 10000 + 5;
char s[maxn];

int main(int argc, char const *argv[])
{
    while (scanf("%s", s) == 1)
    {
        list<char> chlist;
        for (int i=0, n=strlen(s); i<n ;i++) {
            if (s[i] == '[')    ;
            else if()
        }
    }
    
    /* code */
    return 0;
}
