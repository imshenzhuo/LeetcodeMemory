#include <algorithm>
#include <iostream>
#include <climits>

using namespace std;

int main(int argc, char const *argv[])
{
    cout << INT_MAX << endl;
    cout << INT_MIN << endl;
    cout << INT_MAX - INT_MAX / 10 << endl;
    cout << INT_MAX - INT_MAX / 10 << endl;
    cout << INT_MAX / 10 * 10 + 7 << endl;
    return 0;
}
